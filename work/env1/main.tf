resource "aws_s3_bucket1" "s3_bucket" {
  bucket = var.bucket_name
  version = "0.3.16"
  tags = var.tags
}

resource "aws_s3_bucket2" "s3_bucket" {
  bucket = var.bucket_name
  version = "0.3.16"
  tags = var.tags
}

resource "aws_s3_bucket2" "s3_bucket" {
  bucket = var.bucket_name
  version = "0.3.16"
  tags = var.tags
}

resource "aws_s3_bucket2" "s3_bucket" {
  bucket = var.bucket_name
  version = "0.3.16"
  tags = var.tags
}
